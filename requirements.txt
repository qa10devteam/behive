# BeHive core dependencies
# For optional features: pip install behive[all]
aiohttp>=3.9
httpx>=0.25
psycopg2-binary>=2.9
pydantic>=2.0
litellm>=1.40
feedparser>=6.0

# API server (included in pip install behive[api])
fastapi>=0.100
uvicorn>=0.20

# MCP server (included in pip install behive[mcp])
fastmcp>=3.0
